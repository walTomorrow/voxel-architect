import type { GeneratorPlacement } from "@/src/lib/generation/placement/placementUtils";

/** v1 massing anchor — emits zero placements. */
export function emitRectangularBody(): GeneratorPlacement[] {
  return [];
}
